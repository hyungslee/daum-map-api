import React, { Component } from "react";

export default class Marker extends Component {
  constructor(props) {
    super(props);
    this.state = {
      markers: []
    };
  }

  render() {
    return <div />;
  }
}
